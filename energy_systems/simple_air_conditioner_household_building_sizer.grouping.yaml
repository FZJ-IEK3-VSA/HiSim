# The grouping decision for system_setups/simple_air_conditioner_household_building_sizer.py: what each difference between its probe
# configurations means. Edited through the workbook, committed in this form.
setup: system_setups/simple_air_conditioner_household_building_sizer.py
probes: energy_systems/simple_air_conditioner_household_building_sizer.probes.yaml
assignments:
  Building:
    assign: override
    note: Only the TABULA code moves. The envelope behind it - the U-values, the areas,
      the heat capacity class - is looked up by the component itself and never written
      into the file, so one archetype instead of another is a value a consumer picks
      and the component and its wiring are the same either way. It is the only row
      in this table because the air conditioner and its controller are built from
      their own class defaults and no sizing context reaches them.
configurations:
  baseline: {}
  nineties_building: {}
