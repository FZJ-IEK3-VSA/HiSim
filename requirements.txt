numpy
pandas==3.0.5
matplotlib
seaborn
reportlab
pvlib # ==0.10.2
oemof.thermal
openpyxl
pytest
sphinx
sphinx-rtd-theme
dataclasses_json
hplib==1.9
bslib==0.6
psutil
pydot
graphviz
utspclient==0.1.6
html2image
control
casadi
plotly
ordered_set
typing_extensions
python-dotenv
windpowerlib # ==0.2.1
timezonefinder<6.0
#wetterdienst==0.63.0 # TODO: migrate weather_data_import.py to new API before upgrading
cdsapi
xarray
pygfunction
pyloadprofilegenerator
portalocker
pyhumps==3.8.0
pydantic
# Energy-system files (hisim/energy_system): the YAML read and write path
PyYAML
# Energy-system run records (hisim/energy_system/comments.py): write-only, for the provenance
# comments of a realized record; the read path stays on PyYAML's safe loader
ruamel.yaml
# Energy-system schema (tests/test_energy_system_schema.py): validates the exported JSON Schema
# and the mockups against it; HiSim itself never imports it at runtime
jsonschema
Pillow
# Worked-example library: tools/convert_worked_examples.py writes the YAML fixtures,
# tests/test_worked_examples.py reads them (cost-spec-v2 §3.3).
pyyaml
# Independent implementation cross-check of the closed-form financial math
# (tests/test_economics_invariants.py, cost-spec-v2 §5.3).
numpy_financial
# RenoVisor translator (hisim/renovisor): REST result submission
requests
# HPC harness (scripts/hpc_harness): REST queue server + worker fleet
fastapi
uvicorn
httpx
